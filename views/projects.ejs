<!DOCTYPE html>
<html lang="en" data-theme="dim">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Projects - Climate Solutions</title>
  <link rel="stylesheet" href="/css/main.css">
</head>
<body>
  <%- include('partials/navbar') %>

  <div class="container mx-auto px-4 py-12">
    <div class="text-center mb-12">
      <h1 class="text-5xl font-bold mb-4">
        <%= currentSector ? currentSector + " Solutions" : "All Climate Solutions" %>
      </h1>
      <p class="text-xl opacity-80">Showing <%= projects.length %> project<%= projects.length !== 1 ? 's' : '' %></p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      <% projects.forEach(project => { %>
        <div class="card bg-base-200 shadow-xl hover:shadow-2xl transition-all">
          <figure class="h-64">
            <img src="<%= project.feature_img_url %>" alt="<%= project.title %>" class="w-full h-full object-cover rounded-t-box">
          </figure>
          <div class="card-body">
            <div class="badge badge-primary mb-3"><%= project.Sector.sector_name %></div>
            <h2 class="card-title text-2xl"><%= project.title %></h2>
            <p class="text-base opacity-80 line-clamp-3"><%= project.summary_short %></p>

            <!-- EDIT + DELETE BUTTONS RIGHT HERE -->
            <div class="card-actions justify-between mt-6">
              <a href="/solutions/project/<%= project.id %>" class="btn btn-primary">Learn More →</a>
              <div class="flex gap-2">
                <a href="/solutions/editProject/<%= project.id %>" class="btn btn-success btn-sm">Edit</a>
                <a href="/solutions/deleteProject/<%= project.id %>" 
                   class="btn btn-error btn-sm" 
                   onclick="return confirm('Are you sure you want to DELETE this project? This cannot be undone!')">
                   Delete
                </a>
              </div>
            </div>
          </div>
        </div>
      <% }) %>
    </div>

    <% if (projects.length === 0) { %>
      <div class="text-center py-20">
        <p class="text-2xl opacity-60">No projects found in this sector.</p>
      </div>
    <% } %>
  </div>
</body>
</html>