<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login - WEB322 A3</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
  <div class="min-h-screen flex items-center justify-center">
    <div class="bg-white p-10 rounded-lg shadow-xl w-96">
      <h2 class="text-3xl font-bold text-center mb-8">WEB322 Assignment 3</h2>
      <% if (error) { %>
        <p class="text-red-600 text-center mb-4"><%= error %></p>
      <% } %>
      <form method="POST">
        <input type="text" name="user" placeholder="Username" required 
               class="w-full p-3 mb-4 border rounded-lg"/>
        <input type="password" name="password" placeholder="Password" required 
               class="w-full p-3 mb-6 border rounded-lg"/>
        <button type="submit" 
                class="w-full bg-indigo-600 text-white p-3 rounded-lg hover:bg-indigo-700">
          Login
        </button>
      </form>
      <p class="text-center text-sm text-gray-600 mt-4">Hint: admin / admin</p>
    </div>
  </div>
</body>
</html>