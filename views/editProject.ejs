<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Project - Climate Solutions</title>
    <link href="/css/main.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <%- include('partials/navbar') %>
    
    <main class="container mx-auto px-4 py-8">
        <div class="bg-gray-200 rounded-lg mb-8 p-8 shadow-md">
            <div class="text-center">
                <div class="max-w-3xl mx-auto">
                    <h1 class="text-5xl font-bold mb-2 text-gray-800">Edit Project</h1>
                    <p class="text-2xl mb-4 text-gray-700 font-semibold"><%= project.title %></p>
                    <p class="text-lg text-gray-600">
                        Update the project details below.
                    </p>
                </div>
            </div>
        </div>

        <div class="bg-white rounded-lg shadow-lg max-w-4xl mx-auto p-8">
            <form action="/solutions/editProject" method="POST" class="space-y-6">
                <input type="hidden" name="id" value="<%= project.id %>">
                
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div class="md:col-span-2">
                        <label class="block text-gray-700 text-sm font-bold mb-2" for="title">
                            Project Title *
                        </label>
                        <input type="text" name="title" id="title" 
                               value="<%= project.title %>"
                               class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-gray-500 focus:border-transparent" required>
                    </div>

                    <div class="md:col-span-2">
                        <label class="block text-gray-700 text-sm font-bold mb-2" for="feature_img_url">
                            Feature Image URL *
                        </label>
                        <input type="url" name="feature_img_url" id="feature_img_url" 
                               value="<%= project.feature_img_url %>"
                               class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-gray-500 focus:border-transparent" required>
                    </div>

                    <div>
                        <label class="block text-gray-700 text-sm font-bold mb-2" for="sector_id">
                            Sector *
                        </label>
                        <select name="sector_id" id="sector_id" 
                                class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-gray-500 focus:border-transparent" required>
                            <option value="1" <%= (project.sector_id == 1) ? "selected" : "" %>>Land Sinks</option>
                            <option value="2" <%= (project.sector_id == 2) ? "selected" : "" %>>Industry</option>
                            <option value="3" <%= (project.sector_id == 3) ? "selected" : "" %>>Transportation</option>
                            <option value="4" <%= (project.sector_id == 4) ? "selected" : "" %>>Electricity</option>
                            <option value="5" <%= (project.sector_id == 5) ? "selected" : "" %>>Food, Agriculture, and Land Use</option>
                        </select>
                    </div>

                    <div class="md:col-span-2">
                        <label class="block text-gray-700 text-sm font-bold mb-2" for="intro_short">
                            Short Introduction *
                        </label>
                        <textarea name="intro_short" id="intro_short" 
                                  class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-gray-500 focus:border-transparent h-32" required><%= project.intro_short %></textarea>
                    </div>

                    <div class="md:col-span-2">
                        <label class="block text-gray-700 text-sm font-bold mb-2" for="summary_short">
                            Short Summary *
                        </label>
                        <textarea name="summary_short" id="summary_short" 
                                  class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-gray-500 focus:border-transparent h-32" required><%= project.summary_short %></textarea>
                    </div>

                    <div class="md:col-span-2">
                        <label class="block text-gray-700 text-sm font-bold mb-2" for="impact">
                            Impact Description *
                        </label>
                        <textarea name="impact" id="impact" 
                                  class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-gray-500 focus:border-transparent h-40" required><%= project.impact %></textarea>
                    </div>

                    <div class="md:col-span-2">
                        <label class="block text-gray-700 text-sm font-bold mb-2" for="original_source_url">
                            Original Source URL *
                        </label>
                        <input type="url" name="original_source_url" id="original_source_url" 
                               value="<%= project.original_source_url %>"
                               class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-gray-500 focus:border-transparent" required>
                    </div>
                </div>

                <div class="flex flex-col sm:flex-row justify-between items-center pt-6 border-t border-gray-200 gap-4">
                    <a href="/solutions/deleteProject/<%= project.id %>" 
                       class="w-full sm:w-auto bg-red-600 hover:bg-red-700 text-white font-bold py-3 px-6 rounded-md focus:outline-none focus:ring-2 focus:ring-red-500 focus:ring-opacity-50 transition duration-200 text-center"
                       onclick="return confirm('Are you sure you want to delete this project? This action cannot be undone.')">
                        Delete Project
                    </a>
                    
                    <div class="flex gap-4 w-full sm:w-auto">
                        <a href="/solutions/projects/<%= project.id %>" 
                           class="w-1/2 sm:w-auto bg-gray-500 hover:bg-gray-600 text-white font-bold py-3 px-6 rounded-md focus:outline-none focus:ring-2 focus:ring-gray-500 focus:ring-opacity-50 transition duration-200 text-center">
                            Cancel
                        </a>
                        <button type="submit" 
                                class="w-1/2 sm:w-auto bg-gray-700 hover:bg-gray-800 text-white font-bold py-3 px-6 rounded-md focus:outline-none focus:ring-2 focus:ring-gray-500 focus:ring-opacity-50 transition duration-200">
                            Update Project
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </main>
</body>
</html>